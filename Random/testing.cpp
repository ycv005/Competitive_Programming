#include<iostream>
#include<ctype.h>
#include<sstream>
#include<string>
#include<algorithm>
using namespace std;

int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        vector<string> alpha;
        for(int i=0;i<n;i++){
            string s;
            cin>>s;
            sort(s,s+s.size())
            aplha.push_back(s); //pushing the sorted string
        }
        for(int j=0;j<n;j++){
            for(auto i=alpha.begin();i!=aplha.end():i++){
                
            }
        }
    }

    return 0;
}